---
alwaysApply: true
---

Мы пишем проект для того, чтобы преподователям физкультуры в нашем вузе было удобно составлять расписание.
Основная идея проекта заключается в умной таблице расписания, которая подскажет если что-то не так и поможет людям сэкономить своё ценное время.
В функционал входит: добавление отделов (футбол, плавание), преподавателей, факультетов и пар.
В один день есть несколько пар для разных факультетов, на них нужно назначать преподавателей.
У каждого преподавателя есть величина ставки. Полная ставка - 12 занятий в неделю.

# Общие правила проекта
- Используй clean code подход: читаемость важнее краткости.
- Пиши код модульно и разделяй ответственность.
- Следи за единым стилем кода:
  - В Go: gofmt / golangci-lint (camelCase, явные типы).
  - В Svelte: 2 пробела, kebab-case для файлов компонентов.
- Всегда добавляй комментарии к сложной бизнес-логике.
- Не пиши комментарии просто так: только в сложных местах.

# Backend (Go + Wails)
- Используй idiomatic Go (ошибки через error, а не panic).
- Все публичные функции должны возвращать унифицированный ответ:
  {
    data: any,   // результат запроса
    error: string // описание ошибки, "" если ошибки нет
  }
- Для работы с временем используй стандартный пакет time.
- Для экспорта:
  - XLSX → github.com/xuri/excelize
  - PDF → github.com/jung-kurt/gofpdf
  - Разделяй слои: handlers, services, models.
- Не смешивай UI-логику Wails и бизнес-логику.

# Frontend (Svelte)
- Структурируй проект в src/lib/components, src/lib/stores, src/routes.
- Используй файл локализации для 
- Для таблицы расписания:
  - Хранить состояние в Svelte store.
  - Обновления синхронизировать с backend через Wails API.
  - Поддерживать редактирование ячеек инлайн.
- Для экспорта добавь кнопку, вызывающую Go-функцию через Wails API.
- Используй TailwindCSS для стилей (чистые и минималистичные).
- Все стили должны быть консистентные, проверяй соотвествие прошлым стилям перед тем, как писать новые.
- Обязательно учитывай доступность (a11y) при разработке интерфейса.

# Тестирование и надежность
- Всегда проверяй edge cases (например, если у преподавателя нет пар).
- При ошибках backend возвращай error, а frontend обязан отобразить уведомление.
- Не пиши тесты, они нам не нужны 👻

# Дополнительно
- Код должен быть кроссплатформенным (Linux, macOS, Windows). Мы ориентируемся на Windows, но для разработки должны поддерживаться все эти платформы.
- Пиши README для своего кода только если это очень нужно.
- Держи зависимости минимальными, избегай тяжёлых библиотек.
